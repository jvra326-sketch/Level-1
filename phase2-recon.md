# Phase 2: Network Reconnaissance & Port Scanning

- **Network Interfaces**: Verified local IP address assignment (`10.95.178.134`) and active loopback/tunnel interfaces via `ifconfig`.
- **Port Scanning**: Scanned local ports 1-1024 using Netcat (`nc`), confirming standard secure service states.
- **Listening Services**: Inspected active socket bindings via `netstat`, identifying active services on ports 56683, 20241, and 8021.
-
